import { useContext } from 'react';
import { AddLocationContext } from '../../context/AddLocation';

export function AddLocationModal () {
  const { openModal } = useContext(AddLocationContext);

  
}
